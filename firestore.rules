
rules_version = '2';
service cloud.firestore {
match /databases/{database}/documents {
// Per-user encrypted blobs remain private
match /users/{uid}/{coll}/{doc} {
allow read, write: if request.auth !